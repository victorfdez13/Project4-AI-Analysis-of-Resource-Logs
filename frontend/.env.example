# Base URL of the ASP.NET backend (see backend/Properties/launchSettings.json).
# The frontend never calls the Python ai-service directly; the backend
# orchestrates SQL Server retrieval, context construction and the AI call.
VITE_BACKEND_URL=http://localhost:5005
