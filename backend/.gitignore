bin/
obj/
**/*.user
.vs/
*.suo

# Local user store (passwords + api keys). Seeded from appsettings on first run.
users.json

# Local dataset registration list. Seeded from appsettings on first run.
datasets.json

# Temporary local build outputs
build_verify/
temp-build/

# Local restart/runtime logs
*.err.log
*.out.log
